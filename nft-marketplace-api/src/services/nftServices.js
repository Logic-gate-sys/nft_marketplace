import config from '../config/db.js';
const { pool, } = config;

/*
✅ Minting logic:

Validate that the creator has permission.

Save NFT metadata to IPFS or storage.
Insert NFT record into DB.
*/


